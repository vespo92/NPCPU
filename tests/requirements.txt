# Test Requirements for NPCPU

# Core testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Required for NPCPU protocols
numpy>=1.24.0
pyyaml>=6.0

# Optional but recommended for better test output
pytest-html>=3.2.0
pytest-xdist>=3.3.1  # Parallel test execution

# For transformation tests (optional, falls back to pure Python)
scikit-learn>=1.3.0

# For storage adapter tests (optional)
chromadb>=0.4.0  # Only if testing ChromaDB adapter
